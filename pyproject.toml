[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "groww-pulse"
version = "0.1.0"
description = "Weekly Product Review Pulse"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["agent", "play_store_mcp"]
